/*
Write an async function called say_hello() that just prints "Hello, async!". Call it from main() using tokio::main.

Write an async function that waits 1 second using tokio::time::sleep() and then prints "Waited 1 second".

Spawn two tasks using tokio::spawn() — each task should print a different message after a short delay.

Write two async functions that return numbers. Use tokio::join!() to call them in parallel and print the total sum.
*/
fn main(){}